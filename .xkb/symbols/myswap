partial modifier_keys
xkb_symbols "swapkeys" {

// Henkan to Esc
  replace key <XFER> { [ Escape ] };

// CapsLock to Muhenkan: toggle-key of my emacs skk
//  key <CAPS> { [ Muhenkan, Muhenkan ] };

// Muhenkan to Ctrl
  replace key <NFER> { [ Control_L ] };
  modifier_map Control { <NFER> };


};
